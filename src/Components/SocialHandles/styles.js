import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginBottom: 8,
  },
  logo: {
    bottom: 36,
    left: 64,
  },
  text: {
    position: 'absolute',
    marginTop: 10,
    marginLeft: 100,
    fontSize: 15,
    color: 'white',
  },
});

export default styles;
