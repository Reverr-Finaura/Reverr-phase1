import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  docText: {
    fontWeight: '500',
    fontSize: 16,
    color: 'white',
    left: 2,
    marginTop: 240,
  },
  rectangle: {
    marginTop: 40,
    alignItems: 'center',
  },
  upload: {
    position: 'absolute',
    width: 35,
    height: 32,
    top: 50,
    left: 176,
  },
});

export default styles;
